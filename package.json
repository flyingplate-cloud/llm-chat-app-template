{
  "name": "casaos-cloudflare-worker",
  "description": "A comprehensive home server management application for Cloudflare Workers that mimics CasaOS functionality",
  "version": "1.0.0",
  "cloudflare": {
    "account_id": "your-account-id",
    "zone_id": "your-zone-id"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0",
    "vitest": "^1.0.0"
  },
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "check": "tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  }
}
